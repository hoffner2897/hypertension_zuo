# BPHealth 1.0（Build 4）发布日志

时间：2026-08-11（Europe/London）

基线：GitHub 提交 `4df3a92`（修复三餐弹窗关闭并更新低台阶文案），发布提交以最终 Git SHA 为准。

## 发布内容

- 包含 BPHealth Build 3 的完整 iOS、Node API 与 PostgreSQL 改版。
- 修复早餐、午餐、晚餐记录弹窗首次打开时右上角关闭按钮无响应的问题。
- 仅删除两个场景下“低台阶踏步”中“首次练习建议查看动作视频”的提示，其他运动提示保持不变。
- 保持结构化餐食分析的五项后端字段与两张前端结果卡片。
- iOS 版本保持 `1.0`，构建号升级为 `4`。

## 发布目标

- GitHub：`hoffner2897/hypertension_zuo` 的 `pre-approve` 分支。
- Render：`bphealth-api-staging`，根目录 `server`，健康检查 `/health`。
- TestFlight：BPHealth 1.0（Build 4）。

## 验证记录

- Node API：`npm run check && npm test` 通过；共 31 项，29 项通过，2 项真实环境 E2E 按设计跳过，0 项失败。
- iOS 单元测试：iPhone 14 Pro 模拟器通过；确认两个低台阶运动均不再显示视频提示。
- iOS 界面测试：三餐弹窗关闭、iPhone 14 Pro 响应式布局、低门槛运动流程、最近读数文案与头像移除等关键用例通过。
- Render：部署 `dep-d9tc1cnavr4c73bihngg` 成功，线上代码提交为 `51b4f69`。
- Render 构建修复：生产环境会跳过 `devDependencies`，构建命令已改为 `npm ci --include=dev && npm run db:generate && npm run build`，确保 TypeScript 与类型声明可用。
- Prisma：10 个 migration 均处于已应用状态；本次新增的账户研究数据保留与结构化餐食分析 migration 已成功执行。
- 健康检查：`GET /health` 返回 HTTP 200、`ok: true`、`recognitionMode: openai`。
- 真实线上 E2E：注册、登录、刷新、退出、资料、读数、离线同步、GPT 读数解读、GPT 餐食分析、GPT 趋势调整建议和账户删除流程全部通过。
- 血压计图片 GPT：真实线上请求返回 HTTP 200，单次测试约 25 秒。
- iOS Release Archive：成功，归档路径 `/tmp/BPHealthBuild4.xcarchive`。
- TestFlight：Build 4 上传成功，App Store Connect 已接收并进入处理阶段。
